ignored:
  - DL3008  # Pin versions in apt-get install (tool versions managed via ARG VERSION)
  - DL3013  # Pin versions in pip install
  - DL3059  # Multiple consecutive RUN instructions (acceptable for clarity)
